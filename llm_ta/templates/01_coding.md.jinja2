# {{ project_name }} - 初始编码

> 请仔细阅读下列编码，勾选您认为有意义的编码 `[x]`，修改文字，或添加新的编码。
> **注意**: 请勿修改 `<!-- ID: ... -->` 注释中的内容。

## 编码列表

{% for code in codes %}
- [ ] **{{ code.id }}**: {{ code.text }}
  - 原文: "{{ code.source_quote }}"
  <!-- ID: {{ code.id }} | P: {{ code.participant_id }} -->

{% endfor %}

---

## 使用说明

1. 使用 `[x]` 勾选有意义的编码
2. 可以直接修改编码文本来润色或纠正
3. 可以在列表末尾添加新的编码（保持相同格式）
4. 完成后运行 `llm-ta theming` 生成主题
